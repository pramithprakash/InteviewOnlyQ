import "./styles.css";
import $ from "jquery";


/*
let group = {
  title: "Our Players",
  players: ["John", "Pete", "Alice"],

  showList() {
    this.players.forEach(function (player) {
      alert(this.title + ": " + player);
    });
  }
};

group.showList();
*/
